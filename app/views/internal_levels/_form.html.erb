<%= form_for([@company, @level], url:company_internal_levels_url) do |f| %>
  <%= render 'shared/errors', object: @level %>

  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>

  

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>